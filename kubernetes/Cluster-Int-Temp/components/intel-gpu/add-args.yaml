apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: intel-gpu-plugin
  namespace: kube-system
spec:
  template:
    spec:
      containers:
      - name: intel-gpu-plugin
        args:
        - "-shared-dev-num=5"
        - "-enable-monitoring"
        - "-v=2"