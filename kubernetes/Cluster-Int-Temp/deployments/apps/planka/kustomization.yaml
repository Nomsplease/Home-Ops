apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- pvc.yaml
- helm-repo.yaml
- helm-release.yaml
secretGenerator:
  - name: values-planka
    namespace: apps
    files:
      - values.yaml=values.sops.yaml
configurations:
  - kustomizeconfig.yaml