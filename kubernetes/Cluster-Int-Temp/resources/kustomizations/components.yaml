apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: components
  namespace: flux-system
spec:
  interval: 5m0s
  path: ./kubernetes/Cluster-Int-Temp/components/
  prune: true
  sourceRef:
    kind: GitRepository
    name: gitrepo-home-ops
  decryption:
    provider: sops
    secretRef:
      name: sops-age