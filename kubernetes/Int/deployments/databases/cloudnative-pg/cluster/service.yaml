---
apiVersion: v1
kind: Service
metadata:
  name: cnpg-db-cluster
spec:
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    cnpg.io/cluster: cnpg-db-cluster
    role: primary
