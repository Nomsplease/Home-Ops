apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: esphome
spec:
  ingressClassName: ingress-nginx
  rules:
  - host: esphome.svc.noms.tv
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          serviceName: esphome
          servicePort: 6052