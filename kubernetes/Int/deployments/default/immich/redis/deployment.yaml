---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: immich-redis
  namespace: apps
spec:
  replicas: 1
  progressDeadlineSeconds: 600
  revisionHistoryLimit: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: immich-redis
  template:
    metadata:
      labels:
        app: immich-redis
    spec:
      containers:
        - name: immich-redis
          image: redis:7.2.5
          volumeMounts:
            - name: pvc
              mountPath: /data
              subPath: redis
      volumes:
        - name: pvc
          persistentVolumeClaim:
            claimName: immich