---
service:
  type: ClusterIP

ingress:
  ingressClassName: traefik
  enabled: true
  hosts:
    - host: "portainer.noms.tv"
      paths:
        - path: "/"

persistence:
  enabled: true
  existingClaim: portainer