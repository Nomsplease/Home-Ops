apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- database.sops.yaml
- ingress.sops.yaml
secretGenerator:
  - name: gitea-values
    namespace: source-control
    files:
      - values.yaml=gitea-values.yaml