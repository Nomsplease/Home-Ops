apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: cluster-gitrepo
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: testing-sops-apps
  secretRef:
    name: github-deploy-key
  url: ssh://git@github.com/Nomsplease/Home-Ops.git