apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: helm-release-nfs-subdir-external-provisioner
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: kube-system
  chart:
    spec:
      chart: nfs-subdir-external-provisioner
      version: '4.0.18'
      sourceRef:
        kind: HelmRepository
        name: helm-charts-nfs-subdir-external-provisioner
        namespace: flux-system
      interval: 1m
  valuesFrom:
    - kind: Secret
      name: values-nfs-subdir-external-provisioner