apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metallb-config
  namespace: metallb-system
spec:
  dependsOn:
    - name: controller
      namespace: metallb-system
  path: "./config/"