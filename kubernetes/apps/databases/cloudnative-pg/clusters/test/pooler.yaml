apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: pooler-test-rw
spec:
  cluster:
    name: test-db
  instances: 1
  type: rw
  pgbouncer:
    poolMode: session
    parameters:
      max_client_conn: "1000"
      default_pool_size: "10"