---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/pushsecret_v1alpha1.json
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: timescale-app
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: onepassword-connect
      kind: ClusterSecretStore
  selector:
    secret:
      name: timescale-app
  template:
    engineVersion: v2
    data:
      TIMESCALE_APP_USERNAME: '{{ index . "username" }}'
      TIMESCALE_APP_PASSWORD: '{{ index . "password" }}'
  data:
    - match:
        secretKey: &key TIMESCALE_APP_USERNAME
        remoteRef:
          remoteKey: cloudnative-pg
          property: *key
    - match:
        secretKey: &key TIMESCALE_APP_PASSWORD
        remoteRef:
          remoteKey: cloudnative-pg
          property: *key