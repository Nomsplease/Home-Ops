---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lidarr
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: lidarr-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        LIDARR__AUTH__APIKEY: "{{ .LidarrAPIKey }}"
        LIDARR__POSTGRES__USER: "{{ .appUser }}"
        LIDARR__POSTGRES__PASSWORD: "{{ .appPassword }}"
  dataFrom:
    - extract:
        key: arr-apps
    - extract:
        key: cloudnative-pg