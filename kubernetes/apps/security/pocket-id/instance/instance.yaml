# yaml-language-server: $schema=https://raw.githubusercontent.com/aclerici38/pocket-id-operator/refs/heads/main/dist/schemas/pocketidinstance_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDInstance
metadata:
  name: pocket-id
spec:
  annotations:
    reloader.stakater.com/auto: "true"
  image: ghcr.io/pocket-id/pocket-id:v2.12.0-distroless@sha256:b2fad8a82fab26fe4c11797dd13ea55aa6c7d6ea93fe829a139f2e28be2d339d
  versionCheckDisabled: true
  env:
    - name: TZ
      value: America/New_York

  analyticsDisabled: true

  encryptionKey:
    valueFrom:
      secretKeyRef:
        name: pocket-id-secret
        key: encryptionKey
  databaseUrl:
    valueFrom:
      secretKeyRef:
        name: pocket-id-postgres
        key: url
  hostUsers: true
  fileBackend: database

  persistence:
    enabled: false
    extraVolumeMounts:
      - name: postgres-certs
        mountPath: /var/run/secrets/postgresql
        readOnly: true
      - name: root-ca
        mountPath: /var/run/secrets/root-ca
        readOnly: true

  podTemplate:
    spec:
      volumes:
        - name: postgres-certs
          secret:
            secretName: postgres-${APP}-cert
            defaultMode: 0600
        - name: root-ca
          secret:
            secretName: root-ca
            defaultMode: 0440

  logging:
    level: info
    json: false
  # todo: service monitor for this
  metrics:
    enabled: true

  auditLogRetentionDays: 180

  appUrl: "https://id.${SECRET_DOMAIN}"
  route:
    enabled: true
    parentRefs:
      - name: envoy-external
        namespace: network
    hostnames: ["id.${SECRET_DOMAIN}"]

  smtp:
    host: smtp.protonmail.ch
    port: 587
    tls: starttls
    skipCertVerify: false
    from: &from "admin@${SECRET_DOMAIN}"
    user: *from
    password:
      valueFrom:
        secretKeyRef:
          name: pocket-id-secret
          key: smtpToken

  emailNotifications:
    apiKeyExpiration: true
    loginNotification: false
    oneTimeAccessAsAdmin: true
    oneTimeAccessAsUnauthenticated: false
    verification: true

  ui:
    appName: "Pocket ID"
    sessionDuration: 60
    homePageUrl: "/settings/apps"
    disableAnimations: false

  userManagement:
    allowUserSignups: "withToken"

  geoip:
    maxmindLicenseKey:
      valueFrom:
        secretKeyRef:
          name: pocket-id-secret
          key: maxmindLicenseKey