---
# yaml-language-server: $schema=https://crd.movishell.pl/postgresql.cnpg.io/database_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: ${APP}-db
  namespace: "${DB_NS:=databases}"
  labels:
    app: ${APP}-db
spec:
  cluster:
    name: "${CNPG_CLUSTER:=main-db}"
  name: ${APP}
  owner: "${CNPG_DB_OWNER:=app}"