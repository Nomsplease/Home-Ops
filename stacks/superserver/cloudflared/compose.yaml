services:
  cloudflared:
    #command: tunnel run
    command: echo $TUNNEL_TOKEN
    image: cloudflare/cloudflared:2025.4.2
    networks:
      - traefik_proxy
    restart: unless-stopped
networks:
 traefik_proxy:
   name: traefik_proxy
   external: true
